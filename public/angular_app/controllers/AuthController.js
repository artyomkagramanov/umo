angular.module('umo')
  .controller('AuthController', AuthController)

  AuthController.$inject = ['$http'];

    function AuthController($http) {

        
        function init() {
        }

        init();
};